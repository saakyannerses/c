#include <stdio.h>
#include <conio.h>
void main()
int i; // счетчик циклов
printf("Вычисление частичной суммы ряда: " ) ; 
printf("1,3,6,9, ...\n");
printf("Введите количество суммируемых членов -> "); s
canf("%i", &n);
е = 1;
for (i = 1; i <= n; i++)
{
    summ += e;
    e += 2; 
}
printf("Сумма первых %i членов ряда равна %i", n, summ);
printf("\nДля завершения нажмите <Enter>");
getch(); 
}
